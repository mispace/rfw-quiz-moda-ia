/**
 * config.js — Configuração do frontend
 *
 * Este arquivo contém APENAS a URL pública do backend.
 * Nenhuma API key, nenhum segredo.
 * É seguro commitar no repositório.
 *
 * COMO USAR:
 *   1. Copie este arquivo: cp config.example.js config.js
 *   2. Substitua a URL abaixo pela URL real do seu Google Apps Script
 *   3. Teste abrindo index.html no navegador
 */
const CONFIG = {
  backendUrl: 'https://script.google.com/macros/s/COLE_SUA_URL_AQUI/exec'
};
